name: CI

on:
  push:
    paths:
      - "src/**"
      - "tests/**"
      - "pyproject.toml"
      - ".github/workflows/**"
      - "atlas/**"
      - "site/package.json"
      - "site/package-lock.json"
      - "site/astro.config.mjs"
  pull_request:
    paths:
      - "src/**"
      - "tests/**"
      - "pyproject.toml"
      - ".github/workflows/**"
      - "atlas/**"
      - "site/package.json"
      - "site/package-lock.json"
      - "site/astro.config.mjs"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
      # The committed Atlas map (atlas/) must match the tree. Atlas needs
      # Node 22 or later, and the map is the same on every cell, so it is
      # checked on the 3.12 cell only.
      - name: Set up Node for Atlas
        if: matrix.python-version == '3.12'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '22'
      - name: Atlas check
        if: matrix.python-version == '3.12'
        run: npx --yes @dogfood-lab/atlas@1.21.0 check
      - run: uv python install ${{ matrix.python-version }}
      # All extras EXCEPT the heavy optional ones: `nli`/`all` bundle torch + transformers, and
      # `bench` pulls HuggingFace `datasets` (+ pyarrow). All three have tests written to run
      # WITHOUT the extra (test_nli.py is torch-free; test_codejudgebench.py uses the committed
      # offline fixture, no `datasets`), so installing them only slows/reddens CI. Keep CI lean.
      # The provider/mcp/http extras are still installed individually.
      - run: uv sync --all-extras --no-extra nli --no-extra all --no-extra bench
      - run: uv run ruff check src/ tests/
      - run: uv run mypy src/
      - run: uv run pytest --tb=short
